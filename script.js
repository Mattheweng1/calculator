// Basic operator functions

const add = (x,y) => x + y;

const subtract = (x,y) => x - y;

const multiply = (x,y) => x * y;

const divide = (x,y) => x / y;

let numStrOne = "";
let operatorStr = "";
let numStrTwo = "";

let displayStr = `${numStrOne}${operatorStr}${numStrTwo}`;